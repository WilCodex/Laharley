---
import { Image } from "astro:assets";
import Lau from "../../img/laus.png";
---

<section class="hero">
  <div class="hero__background hidden">
    <video src="/video/1.mp4" autoplay muted loop> </video>
  </div>
  <div class="hero__content hidden__reversed">
    <div class="hero__title">
      <div class="hero__title--heading">
        <h1 class="title">Laura Perez || Editora de videos</h1>
      </div>
      <div class="hero__title--text">
        <p>
          ¡Hola! Soy Laura Pérez, una apasionada editora de vídeos con un
          enfoque único y creativo.
        </p>
      </div>
      <div class="btn__container">
        <a href="/" class="btn">Contactame</a>
      </div>
    </div>
    <div class="hero__images">
      <Image src={Lau} alt="Hero image" />
    </div>
  </div>
</section>
<style>
  .hero {
    position: relative;
    width: 100%;
    display: flex;
  }
  .hero__background {
    flex: 3;
    overflow: hidden;
  }
  .hero__content {
    flex: 1;
  }

  .hero__title {
    padding: 1rem;
    flex: 1;
    display: flex;
    gap: 1rem;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    color: var(--primary-color);
  }
  .hero__title--heading {
    width: 100%;
    font-size: clamp(1.5rem, 1.5vw, 4rem);
  }
  .hero__title--text {
    width: 100%;
    font-weight: bold;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  .btn {
    text-decoration: none;
    text-transform: uppercase;
    color: var(--bg-primary);
    background: var(--primary-color);
    padding: 0.75rem;
    font-weight: bolder;
    width: 100%;
    display: inline-block;
    position: relative;
    z-index: 1;
    transition: 0.5s ease;
  }
  .btn:hover {
    color: var(--bg-primary);
    transform: scale(1.1);
  }
  .btn::before {
    content: "";
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    z-index: -1;
    transform-origin: left;
    transition: 0.5s transform ease;
    background: var(--bg-secondary);
    transform: scaleX(0);
  }
  .btn:hover::before {
    transform: scaleX(1);
  }
  .hero__images {
    border-radius: 50%;
    overflow: hidden;
    background: var(--primary-color);
    flex: 1;
    height: 60%;
  }
  .hero__images img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }
  video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  @media screen and (max-width: 768px) {
    .hero {
      flex-direction: column;
    }
    .hero__title {
      width: 100%;
    }
    .hero__title--heading {
      text-align: center;
    }
    .hero__images {
      width: 100%;
    }
  }
</style>
